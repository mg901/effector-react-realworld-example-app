version: 1
update_configs:
  - package_manager: 'javascript'
    directory: '/'
    update_schedule: 'daily'
    ignored_updates:
      - match:
          dependency_name: 'webpack*'
      - match:
          dependency_name: 'history'
      - match:
          dependency_name: '@types/history'
