// import { fork, allSettled } from 'effector';
// import * as store from './store';

describe('pages/article/store', () => {
  it('should return an error if you submit a form with empty fields', async () => {
    // const expected = { errors: { body: ["can't be blank"] } };
    // store.addCommentFx.use(() => Promise.reject(expected));
    // const scope = fork();
    // await allSettled(store.addCommentFx, { scope });
    // expect(scope.getState(store.$error)).toMatchObject(expected);
  });
});
