// import { fork, allSettled } from 'effector';
// import * as store from './store';

describe('user/store', () => {
  it('should return an error if you submit a form with an empty password', async () => {
    // const expected = {
    //   errors: {
    //     password: ["can't be blank"],
    //   },
    // };
    // store.updateUserFx.use(() => Promise.reject(expected));
    // const scope = fork();
    // await allSettled(store.updateUserFx, { scope });
    // expect(scope.getState(store.$error)).toMatchObject(expected);
  });
});
